# YU AI Agent Frontend

快速启动（Windows 环境）：

```powershell
cd e:\java\yu-ai-agent\yu-ai-agent-frontend
npm install
npm run dev
```

应用将运行在 `http://localhost:3000`，前端请求基准地址已配置为 `http://localhost:8123/api`。
